---
driver:
  name: vagrant
  provider:
    name: libvirt
platforms:
  - name: openbsd
    box: pygolo/openbsd7
    memory: 4096
    cpus: 2
provisioner:
  name: ansible
  inventory:
    host_vars:
      openbsd:
        ansible_python_interpreter: /usr/local/bin/python3
